# Blogdex — 작업 핸드오프

> 생성: 2026-07-25 (토)
> 직전 세션: GSC 3계정 분기 + 대시보드 Bing + senior 정리

---

## Current Position

Blogdex 유지보수 세션. 공식 GSD phase 구조 없음 (running maintenance mode).
직전 작업: GSC 데이터 수집 403 오류 수정, 대시보드 확장, 데드코드 정리.

---

## Completed This Session

| # | 작업 | 커밋 |
|---|------|------|
| 1 | **GSC 수집 3계정 분기** — `sync_gsc()`가 사이트 도메인별로 account 1/2/3 credentials 자동 선택 | `852455c` |
| 2 | **keywords.rotcha.kr 제거** — daily_sync, ga4_pageviews, index_submit, hub-worker 4개 파일에서 삭제 + .gitignore 정리 | `23640f9` |
| 3 | **대시보드 Bing 데이터 추가** — worker /dashboard/summary, useDashboardData hook, UserStats component, Dashboard page 4개 파일 수정 | `2037d70` |
| 4 | **senior_briefing.py 호출 제거** — daily_sync sync_senior() 내 불필요한 외부 스크립트 호출 삭제 | `d4f79ef` |
| 5 | **sync_senior() 완전 제거** — Naver API → aikorea24-db 파이프라인 전체 삭제 (118줄). SEAP 발행 파이프라인(5000/pipelines/senior/)과 무관함을 확인 후 제거 | `01c8b36` |

---

## Remaining Work

### 1. 배포 (미배포 상태 — 모든 커밋이 local에만 있음)

| 대상 | 변경 내용 | 배포 명령어 |
|------|----------|------------|
| **API Worker** | `/dashboard/summary`에 Bing 추가, 전반적인 코드 변경 | `cd worker && env -u CLOUDFLARE_API_TOKEN wrangler deploy` |
| **대시보드** | Bing stat cards 추가 (useDashboardData, UserStats, Dashboard) | `cd dashboard && npm run build && wrangler pages deploy ./dist --project-name=blogdex-dashboard` |
| **hub-worker** | keywords.rotcha.kr 제거 (DOMAIN_SITES) | `cd hub-worker && env -u CLOUDFLARE_API_TOKEN wrangler deploy` |
| **cron 반영** | daily_sync.py (GSC 3계정 + senior 제거) | crontab에서 daily_sync.py 경로 확인 후 적용 |

### 2. 잔존 403 — GSC 미등록 (코드 문제 아님, 수동 필요)

| 사이트 | 계정 | 상태 |
|--------|------|------|
| `mimdiomcat.tistory.com` | account=1 | ⏸️ 보류 (GSC 미등록) |

### 3. GA4_PROPERTIES 계정 분기 검토 (정보 수집 완료, action 불필요)

GA4는 단일 credential(account 1)로 826건 정상 수집 중. 메인 계정에 모든 GA4 속성 접근 권한이 부여되어 있어 추가 분기 불필요.

---

## Decisions

1. **GSC 계정 매핑 규칙 확정**:
   - account 1 (twinssn): `rotcha.kr`, `techpawz.com`, `tistory.com` 등
   - account 2 (informationhot): `*.informationhot.kr` 28개
   - account 3 (aikorea24): `*.aikorea24.kr` 5개
   - `_get_account_for_site()` 헬퍼로 도메인 기반 자동 라우팅

2. **senior 데이터 파이프라인 제거 결정**:
   - blogdex의 `sync_senior()` (Naver API → aikorea24-db)는 SEAP 발행 파이프라인(5000/pipelines/senior/, gov24 API → senior.db)과 완전히 별개 시스템
   - senior-hugo 블로그는 SEAP 파이프라인으로 글 발행, blogdex 데이터 미사용

3. **keywords.rotcha.kr 폐기**: 사이트 사용 중단, 전 코드베이스에서 제거

---

## Blockers

- 없음 (배포만 남음)

---

## Bugs & Issues Discovered

| # | 증상 | 원인 | 해결책 | 미해결? |
|---|------|------|--------|---------|
| 1 | GSC 수집 시 informationhot.kr(28개) + aikorea24.kr(5개) 사이트 전부 403 | `sync_gsc()`가 무조건 account=1 credentials로만 호출 — 다른 계정 소유 사이트는 권한 없음 | `_get_account_for_site()` 도메인 매핑 추가, 계정별로 GSC service 생성 | ✅ 해결 |
| 2 | 대시보드에 Bing 데이터 미노출 | `useDashboardData.js`가 `/bing/daily`를 호출하지 않음, `/dashboard/summary`가 Bing 통계 미포함 | hook에 `/bing/daily` 추가, summary에 bing_clicks/impressions 추가, UserStats에 Bing 섹션 추가 | ✅ 해결 |
| 3 | `keywords.rotcha.kr` 사이트 폐기되었으나 코드 전체에 잔존 | 사이트 중단 후 제거 누락 | daily_sync, ga4_pageviews, index_submit, hub-worker 4개 파일에서 삭제 | ✅ 해결 |
| 4 | senior 데이터 수집+브리핑이 매일 실행되나 아무도 사용 안 함 | sync_senior()는 Naver 뉴스 → aikorea24-db → 로컬 HTML 브리핑 생성, SEAP/Hugo 블로그와 무관 | sync_senior() + Naver API 키 로딩 + senior_briefing 호출 전부 제거 | ✅ 해결 |

---

## Environment Notes

- **OS**: macOS (darwin)
- **Python**: daily_sync.py는 system python3 사용
- **Cloudflare**: 3개 계정 (twinssn/hugh79757, informationhot, aikorea24)
- **Wrangler**: OAuth profile `hugh79757` 활성화 (default). `farmsolution` profile 별도 존재.
- **Crontab**: daily_sync.py가 매일 1회 cron으로 실행됨 (정확한 시간 미확인)
- **핵심 환경변수 충돌 방지**: wrangler 명령 실행 시 반드시 `env -u CLOUDFLARE_API_TOKEN` 접두사 필요
  (OpenCode가 `CLOUDFLARE_API_TOKEN`을 자동 설정하여 wrangler OAuth 인증과 충돌)
- **Hugo 테마 경로**: `HUGO_THEMESDIR=/Users/twinssn/Projects/shared-themes`

---

## Resume Instructions

```bash
# 1. 프로젝트 상태 확인
cd /Users/twinssn/Projects/blogdex
git log --oneline -5
git status --short

# 2. 주요 배포 (unpushed commits 5개 있음)
# API Worker:
env -u CLOUDFLARE_API_TOKEN wrangler deploy
# 대시보드:
cd dashboard && npm run build && env -u CLOUDFLARE_API_TOKEN wrangler pages deploy ./dist --project-name=blogdex-dashboard
# hub-worker:
cd hub-worker && env -u CLOUDFLARE_API_TOKEN wrangler deploy
# cron 반영 확인:
crontab -l | grep daily_sync

# 3. 남은 403 확인 (mimdiomcat.tistory.com)
# GSC 콘솔에서 등록 여부 확인

# 4. 다음 cron 실행 결과 확인
tail -f logs/daily_sync.log | grep -E "GSC|403|error"
```

---

## Uncommitted Changes

- 없음 (모든 변경사항 커밋 완료, push만 안 됨)
- 5개 커밋 unpushed: `01c8b36 d4f79ef 2037d70 23640f9 852455c`
